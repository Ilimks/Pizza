import React  from 'react';
import './Loader.css'

const Loader = () => { 

    return (
        <div class="preloader">
            <span class="loader"></span>
        </div>
    );
};

export default Loader;